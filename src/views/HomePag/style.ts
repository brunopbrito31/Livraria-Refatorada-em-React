import styled from 'styled-components';

export const Home = styled.div`
    height:100vh;

    .area-cards{
        margin-top: 2%;
        display: flex;
        flex-wrap: wrap;
        justify-content:space-around;
    }

`